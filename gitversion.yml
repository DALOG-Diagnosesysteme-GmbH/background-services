mode: ContinuousDelivery
branches:
  main:
    regex: ^main$
    increment: Patch
    track-merge-target: false
  unknown:
    regex: (?<BranchName>.+)
    increment: Patch
    tag: 'preview'
commit-message-incrementing: Enabled
